services:
  - type: web
    name: admin-backend
    runtime: node
    buildCommand: cd admin-backend && npm install
    startCommand: cd admin-backend && node server.js
    envVars:
      - key: ENV
        value: production
